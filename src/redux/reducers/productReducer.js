const initialState = {
  products: []
};

export const productsReducer = (state, {type, payload}) => {
  switch (type) {
    case ActionTypes.SET_PRODUCTS:

      break;

    default: 
      break;
  }
}